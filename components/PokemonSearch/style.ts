import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container:{
      alignContent: "center",
      marginTop: 10,
      borderColor: "#000", 
      borderWidth: 1,
      marginHorizontal: 20,
      paddingVertical: 1
    },
  });

export default styles;