import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    list: {
      flex: 1,
      flexDirection: 'row'
    },
    dataBloc : {
      flex:  1,
      paddingVertical : 10,
      paddingHorizontal : 5
    },
    data: {
      flex: 1,
      fontSize: 20,
    }
  });

export default styles;