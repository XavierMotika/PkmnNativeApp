import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
    container: {
      flex: 1
    },
    list: {
      flex: 10,
      alignSelf:"center",
      paddingTop: 5
    }
  });

  export default styles;